{
    "scripts": {
        "test": "jest",
        "bdd": "cucumber-js --format summary --format html:report.html"
    },
    "devDependencies": {
        "jest": "^30.2.0"
    },
    "dependencies": {
        "@cucumber/cucumber": "^12.7.0",
        "express": "^5.2.1",
        "supertest": "^7.2.2"
    }
}
